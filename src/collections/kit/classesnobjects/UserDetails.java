package collections.kit.classesnobjects;

public interface UserDetails {

    void userRegistrationId();  //before java 8 , interface will contain only abstract methods not concrete methods
                                    // after java we do have the concrete method as well
                                // Rules for implementing interface is, need to override all the abstract methods in the implementation calsses

    void add();
}
